git init

git status

git add 

git rm --cached

git restore

git commit -m "adding"

git log

git init

git log --oneline

git log --oneline --pretty

git config --global user.name "Mohammad Piyarji"

git config --global user.email "piyarjimohammad@gmail.com"

git checkout -b dev1

git checkout dev1

git checkout master

git remote -v

git pull origin main

git fetch origin main

git push origin main 

git remote set-url --add origin <newurl>

git remote set-url --delete orgin <newurl>

git merge dev/master

git revert <commit ID>

git reset <commit id>

git diff # will get to know what was the diff there compared to pervious version 

git push origin master --rebase

git rebase master 

git master --squash

git cherry-pick <commit id>

vim .gitignore

