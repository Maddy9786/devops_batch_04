git init

git status

git add 

git rm --cached

git restore

git commit -m "adding"

git log

git init

git log --oneline

git log --oneline --pretty

git config --global user.name "Mohammad Piyarji"

git config --global user.email "piyarjimohammad@gmail.com"

git checkout -b dev1

git checkout master

git remote -v

git pull origin main

git fetch origin main

git push origin main 

